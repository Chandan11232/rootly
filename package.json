{
  "name": "rootly",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered incident root cause analysis for modern infrastructure",
  "scripts": {
    "dev:api": "cd api && npm run dev",
    "dev:worker": "cd worker && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:worker\" \"npm run dev:frontend\"",
    "build": "cd api && npm run build && cd ../worker && npm run build && cd ../frontend && npm run build",
    "install:all": "cd api && npm install && cd ../worker && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
